<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.mapper.CustomerMapper">
    <select id="select" parameterType="string" resultType="mybatis.pojo.Customer">
        select * from t_customer where name=#{name};
    </select>

    <insert id="insert" parameterType="mybatis.pojo.Customer" >
        insert t_customer(id,name,amount) value (#{id},#{name},#{amount})
    </insert>

    <update id="update" parameterType="mybatis.pojo.Customer" >
        update t_customer set name=#{name},amount=#{amount} where id=#{id}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        delete t_customer where id=#{id}
    </delete>
</mapper>